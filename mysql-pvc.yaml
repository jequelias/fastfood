apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  # namespace: cluster-fastfood
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-storage